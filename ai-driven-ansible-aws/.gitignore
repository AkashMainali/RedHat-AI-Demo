# --- Terraform ---
terraform/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
terraform/terraform.tfvars
*.auto.tfvars
tfplan
*.tfplan
# NOTE: commit terraform/.terraform.lock.hcl (it pins provider versions).

# --- Secrets / keys / env (must never be committed) ---
.env
*.env
secrets.*
*.pem
*.key
id_ed25519*
id_rsa*
aiops_ansible_demo*

# --- Ansible generated at runtime ---
ansible/inventory.ini
ansible/.known_hosts
ansible/*.retry
ansible/collections/

# --- OS ---
.DS_Store
